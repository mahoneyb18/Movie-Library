@model Cinema.Models.Genre

@{ 
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Genre List</h2>
            <select asp-for="GenreId" asp-items="@(new SelectList(ViewBag.message,"GenreId","Name"))"></select>
        </div>
        <div class="col-6">
            Create New Genre
        </div>
    </div>

    <br /><br />

    @*
    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>
                        Genre Name
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var obj in Model)
                {
                    <tr>
                        <td width="50%">@obj.Name</td>
                        <td></td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p> No genre exists.</p>
    }
    *@

</div>

